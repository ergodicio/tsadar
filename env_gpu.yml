name: tsadar-gpu
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pyhdf
  - xlrd
  - pip:
      - "jax[cuda12]"
      - "jaxopt>=0.6.0"
      - typing-extensions
      - pynvml
      - -r requirements.txt
      
