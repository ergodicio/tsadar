[pytest]
python_files = tests.py test.py test_*.py *_tests.py
addopts = --strict-markers
filterwarnings = ignore::DeprecationWarning
markers =
    physics: validates a documented physical invariant or trusted reference case
    slow: excluded from pull-request CI and run by the scheduled reference suite
    gpu: requires a real JAX GPU backend
