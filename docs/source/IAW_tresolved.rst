Time Resolved IAW
==============================

This example demonstrates fitting time-resolved data for IAW data and the input deck can be used as a starting point or similar data. 

To run this example, download the data and the decks and the run the code in ``fit`` mode. The output is also provided in the zip file in case you want to make sure you get the same results.

.. image:: _elfolder/fit_and_data_ion.png
    :scale: 85%


::download:`IAW data <examples/time_resolved/IAW-s116773.hdf>` 
::download:`input deck <examples/time_resolved/IAW_tresolved_inputs.yaml>` 
::download:`default deck <examples/time_resolved/IAW_tresolved_defaults.yaml>` 
::download:`output plots <examples/time_resolved/Time_resolved_IAW.zip>`